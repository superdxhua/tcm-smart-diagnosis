# 微信支付配置指南

## 目录
1. [准备工作](#准备工作)
2. [获取商户号和密钥](#获取商户号和密钥)
3. [配置环境变量](#配置环境变量)
4. [代码实现](#代码实现)
5. [测试验证](#测试验证)
6. [常见问题](#常见问题)

---

## 准备工作

### 1. 注册微信支付商户号

1. 访问 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 点击"立即入驻"
3. 填写企业/个体工商户信息
4. 上传营业执照、法人身份证等资料
5. 提交审核（通常 1-3 个工作日）
6. 审核通过后，获得商户号（mch_id）

### 2. 绑定小程序

1. 登录微信支付商户平台
2. 进入"产品中心" -> "AppID 账号管理"
3. 绑定你的小程序 AppID
4. 确认绑定成功

### 3. 申请支付权限

1. 进入"产品中心" -> "我的产品"
2. 申请"小程序支付"产品
3. 提交相关资料审核
4. 审核通过后即可使用

---

## 获取商户号和密钥

### 1. 获取商户号（mch_id）

1. 登录微信支付商户平台
2. 进入"账户中心" -> "商户信息"
3. 查看"微信支付商户号"，格式如：`1234567890`

### 2. 设置 API 密钥（api_key）

1. 登录微信支付商户平台
2. 进入"账户中心" -> "API 安全"
3. 点击"设置 API 密钥"
4. 设置 32 字符的密钥（建议使用随机字符串生成器）
5. 保存密钥，**妥善保管，切勿泄露**

### 3. 下载商户证书

1. 进入"账户中心" -> "API 安全"
2. 点击"下载证书"
3. 下载以下文件：
   - `apiclient_cert.p12`（商户证书）
   - `apiclient_cert.pem`（商户证书公钥）
   - `apiclient_key.pem`（商户证书私钥）
4. 将证书文件放到项目的 `server/certificates/` 目录下

### 4. 获取小程序 AppID 和 AppSecret

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入"开发" -> "开发管理"
3. 查看"开发者ID"（AppID）
4. 查看"开发者密码"（AppSecret），需要管理员扫码获取

---

## 配置环境变量

### 1. 创建环境配置文件

在项目根目录创建 `.env` 文件：

```bash
# 微信支付配置
WECHAT_PAY_APP_ID=wx1234567890abcdef
WECHAT_PAY_MCH_ID=1234567890
WECHAT_PAY_API_KEY=your_32_character_api_key_here

# 证书路径
WECHAT_PAY_CERT_PATH=./certificates/apiclient_cert.p12
WECHAT_PAY_CERT_PASSWORD=your_cert_password

# 回调地址
WECHAT_PAY_NOTIFY_URL=https://your-domain.com/api/payment/callback/wechat

# 支付接口地址（默认正式环境）
WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com

# 如果是沙箱环境，使用以下配置
# WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com/sandboxnew
```

### 2. 创建证书目录

```bash
mkdir -p server/certificates
```

将下载的证书文件放到 `server/certificates/` 目录：
- `apiclient_cert.p12`
- `apiclient_cert.pem`
- `apiclient_key.pem`

### 3. 更新 NestJS 配置

在 `server/src/config/wechat-pay.config.ts` 中配置：

```typescript
export const wechatPayConfig = {
  appId: process.env.WECHAT_PAY_APP_ID,
  mchId: process.env.WECHAT_PAY_MCH_ID,
  apiKey: process.env.WECHAT_PAY_API_KEY,
  certPath: process.env.WECHAT_PAY_CERT_PATH,
  certPassword: process.env.WECHAT_PAY_CERT_PASSWORD,
  notifyUrl: process.env.WECHAT_PAY_NOTIFY_URL,
  apiUrl: process.env.WECHAT_PAY_API_URL || 'https://api.mch.weixin.qq.com',
}
```

---

## 代码实现

### 1. 安装依赖

```bash
pnpm add axios crypto
pnpm add -D @types/crypto-js
```

### 2. 更新微信支付服务

更新 `server/src/payment/wechat-pay.service.ts`，实现真实的微信支付接口：

```typescript
import { Injectable } from '@nestjs/common';
import axios from 'axios';
import * as crypto from 'crypto';
import * as fs from 'fs';
import * as https from 'https';
import { wechatPayConfig } from '@/config/wechat-pay.config';

export interface WeChatPayParams {
  orderNo: string;
  amount: number;
  description: string;
  openId?: string;
}

export interface WeChatPayResult {
  codeUrl?: string;
  prepayId?: string;
  paySign?: string;
  timeStamp?: string;
  nonceStr?: string;
  package?: string;
}

@Injectable()
export class WeChatPayService {
  private config = wechatPayConfig;

  /**
   * 统一下单（创建支付订单）
   */
  async createOrder(params: WeChatPayParams): Promise<WeChatPayResult> {
    const { orderNo, amount, description } = params;

    // 生成随机字符串
    const nonceStr = this.generateNonceStr();

    // 构建请求参数
    const requestParams = {
      appid: this.config.appId,
      mch_id: this.config.mchId,
      nonce_str: nonceStr,
      body: description,
      out_trade_no: orderNo,
      total_fee: Math.round(amount * 100), // 金额转换为分
      spbill_create_ip: '127.0.0.1',
      notify_url: this.config.notifyUrl,
      trade_type: 'NATIVE', // 扫码支付
    };

    // 生成签名
    const sign = this.generateSign(requestParams);

    // 添加签名
    const xmlData = this.buildXml({ ...requestParams, sign });

    // 发送请求
    const response = await this.sendRequest('/pay/unifiedorder', xmlData);

    console.log('微信支付统一下单响应:', response);

    // 解析响应
    const result = this.parseXml(response);

    if (result.return_code === 'SUCCESS' && result.result_code === 'SUCCESS') {
      return {
        codeUrl: result.code_url,
        prepayId: result.prepay_id,
      };
    } else {
      throw new Error(result.err_code_des || '支付订单创建失败');
    }
  }

  /**
   * 查询订单
   */
  async queryOrder(orderNo: string) {
    const nonceStr = this.generateNonceStr();

    const requestParams = {
      appid: this.config.appId,
      mch_id: this.config.mchId,
      out_trade_no: orderNo,
      nonce_str: nonceStr,
    };

    const sign = this.generateSign(requestParams);
    const xmlData = this.buildXml({ ...requestParams, sign });

    const response = await this.sendRequest('/pay/orderquery', xmlData);
    const result = this.parseXml(response);

    if (result.return_code === 'SUCCESS') {
      return {
        orderNo,
        status: this.mapTradeState(result.trade_state),
        transactionId: result.transaction_id,
        tradeState: result.trade_state,
        totalAmount: parseInt(result.total_fee) / 100,
      };
    } else {
      throw new Error(result.err_code_des || '查询订单失败');
    }
  }

  /**
   * 申请退款
   */
  async refund(orderNo: string, refundAmount: number, refundReason: string) {
    const nonceStr = this.generateNonceStr();
    const refundId = `REF${Date.now()}${this.generateNonceStr()}`;

    const requestParams = {
      appid: this.config.appId,
      mch_id: this.config.mchId,
      nonce_str: nonceStr,
      out_trade_no: orderNo,
      out_refund_no: refundId,
      total_fee: Math.round(refundAmount * 100), // 需要传入订单总金额
      refund_fee: Math.round(refundAmount * 100),
      refund_desc: refundReason,
    };

    const sign = this.generateSign(requestParams);
    const xmlData = this.buildXml({ ...requestParams, sign });

    // 退款需要使用证书
    const response = await this.sendRequestWithCert('/secapi/pay/refund', xmlData);
    const result = this.parseXml(response);

    if (result.return_code === 'SUCCESS' && result.result_code === 'SUCCESS') {
      return {
        refundId,
        orderNo,
        refundAmount,
        status: 'SUCCESS',
        refundReason,
      };
    } else {
      throw new Error(result.err_code_des || '退款失败');
    }
  }

  /**
   * 查询退款
   */
  async queryRefund(refundId: string) {
    const nonceStr = this.generateNonceStr();

    const requestParams = {
      appid: this.config.appId,
      mch_id: this.config.mchId,
      nonce_str: nonceStr,
      out_refund_no: refundId,
    };

    const sign = this.generateSign(requestParams);
    const xmlData = this.buildXml({ ...requestParams, sign });

    const response = await this.sendRequest('/pay/refundquery', xmlData);
    const result = this.parseXml(response);

    if (result.return_code === 'SUCCESS') {
      return {
        refundId,
        status: this.mapRefundStatus(result.refund_status),
        refundAmount: parseInt(result.refund_fee) / 100,
        refundReceiveAccount: result.refund_recv_accout,
      };
    } else {
      throw new Error(result.err_code_des || '查询退款失败');
    }
  }

  /**
   * 生成随机字符串
   */
  private generateNonceStr(length = 32): string {
    const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    let result = '';
    for (let i = 0; i < length; i++) {
      result += chars.charAt(Math.floor(Math.random() * chars.length));
    }
    return result;
  }

  /**
   * 生成签名（MD5）
   */
  private generateSign(params: any): string {
    // 1. 参数排序
    const sortedKeys = Object.keys(params).sort();

    // 2. 拼接字符串
    const stringA = sortedKeys
      .filter((key) => params[key] !== undefined && params[key] !== '')
      .map((key) => `${key}=${params[key]}`)
      .join('&');

    // 3. 拼接 API 密钥
    const stringSignTemp = `${stringA}&key=${this.config.apiKey}`;

    // 4. MD5 加密并转大写
    return crypto.createHash('md5').update(stringSignTemp, 'utf8').digest('hex').toUpperCase();
  }

  /**
   * 构建 XML
   */
  private buildXml(params: any): string {
    let xml = '<xml>';
    Object.keys(params).forEach((key) => {
      const value = params[key];
      if (value !== undefined && value !== '') {
        xml += `<${key}><
![CDATA[${value}]]></${key}>`;
      }
    });
    xml += '</xml>';
    return xml;
  }

  /**
   * 解析 XML
   */
  private parseXml(xml: string): any {
    // 简化的 XML 解析，实际项目中建议使用 xml2js
    const result: any = {};
    const regex = /<(\w+)><
!\[CDATA\[(.*?)\]\]><\/\1>/g;
    let match;
    while ((match = regex.exec(xml)) !== null) {
      result[match[1]] = match[2];
    }
    return result;
  }

  /**
   * 发送请求
   */
  private async sendRequest(path: string, xmlData: string): Promise<string> {
    const url = `${this.config.apiUrl}${path}`;

    const response = await axios.post(url, xmlData, {
      headers: { 'Content-Type': 'application/xml' },
    });

    return response.data;
  }

  /**
   * 发送请求（使用证书）
   */
  private async sendRequestWithCert(path: string, xmlData: string): Promise<string> {
    const url = `${this.config.apiUrl}${path}`;

    // 读取证书
    const cert = fs.readFileSync(this.config.certPath);
    const agent = new https.Agent({
      pfx: cert,
      passphrase: this.config.certPassword,
    });

    const response = await axios.post(url, xmlData, {
      headers: { 'Content-Type': 'application/xml' },
      httpsAgent: agent,
    });

    return response.data;
  }

  /**
   * 映射交易状态
   */
  private mapTradeState(tradeState: string): string {
    const stateMap: any = {
      SUCCESS: 'SUCCESS',
      REFUND: 'REFUND',
      NOTPAY: 'NOTPAY',
      CLOSED: 'CLOSED',
      REVOKED: 'REVOKED',
      USERPAYING: 'USERPAYING',
      PAYERROR: 'PAYERROR',
    };
    return stateMap[tradeState] || 'UNKNOWN';
  }

  /**
   * 映射退款状态
   */
  private mapRefundStatus(refundStatus: string): string {
    const statusMap: any = {
      SUCCESS: 'SUCCESS',
      REFUNDCLOSE: 'CLOSED',
      PROCESSING: 'PROCESSING',
      CHANGE: 'CHANGE',
    };
    return statusMap[refundStatus] || 'UNKNOWN';
  }

  /**
   * 验证签名
   */
  verifySign(data: any, sign: string): boolean {
    const computedSign = this.generateSign(data);
    return computedSign === sign;
  }
}
```

### 3. 创建支付回调接口

创建 `server/src/payment/payment.controller.ts` 中的微信支付回调接口：

```typescript
  // 微信支付回调
  @Post('callback/wechat')
  async wechatCallback(@Body() body: any) {
    console.log('微信支付回调:', body);

    try {
      // 解析 XML
      const xmlData = this.weChatPayService.parseXml(body);

      // 验证签名
      if (!this.weChatPayService.verifySign(xmlData, xmlData.sign)) {
        throw new BadRequestException('签名验证失败');
      }

      // 处理回调
      const { out_trade_no, transaction_id, result_code } = xmlData;

      if (result_code === 'SUCCESS') {
        await this.paymentService.handlePaymentCallback(
          out_trade_no,
          transaction_id,
          'paid'
        );
      } else {
        await this.paymentService.handlePaymentCallback(
          out_trade_no,
          transaction_id,
          'failed'
        );
      }

      // 返回成功响应
      return this.buildXmlResponse('SUCCESS', 'OK');
    } catch (error) {
      console.error('微信支付回调处理失败:', error);
      return this.buildXmlResponse('FAIL', error.message);
    }
  }

  /**
   * 构建微信支付响应
   */
  private buildXmlResponse(returnCode: string, returnMsg: string): string {
    return `
      <xml>
        <return_code><
![CDATA[${returnCode}]]></return_code>
        <return_msg><
![CDATA[${returnMsg}]]></return_msg>
      </xml>
    `;
  }
```

---

## 测试验证

### 1. 使用沙箱环境测试

在 `.env` 文件中配置沙箱环境：

```bash
WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com/sandboxnew
```

### 2. 测试流程

1. **创建订单测试**
```bash
curl -X POST http://localhost:3000/api/payment/create-package-order \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "xxx",
    "packageId": "xxx",
    "paymentMethod": "wechat"
  }'
```

2. **扫描二维码测试**
   - 使用微信扫描返回的 `codeUrl`
   - 完成支付流程

3. **查询订单状态**
```bash
curl -X GET http://localhost:3000/api/payment/order/PKGxxx
```

4. **验证支付回调**
   - 支付成功后，微信会自动调用回调接口
   - 检查数据库中的订单状态是否更新

### 3. 正式环境切换

测试通过后，切换到正式环境：

```bash
WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com
```

---

## 常见问题

### 1. 签名错误

**问题：** 返回"签名错误"

**解决方案：**
- 检查 API 密钥是否正确
- 检查参数排序是否正确
- 检查是否正确拼接 `&key=`
- 检查是否转换为大写

### 2. 证书错误

**问题：** 返回"证书错误"

**解决方案：**
- 确认证书文件路径正确
- 确认证书密码正确
- 确认证书文件格式正确（p12 格式）

### 3. 回调接收失败

**问题：** 支付成功后未收到回调

**解决方案：**
- 确认回调地址可公网访问
- 确认回调地址已在微信支付后台配置
- 检查服务器防火墙设置
- 查看微信支付后台的回调日志

### 4. 订单金额错误

**问题：** 订单金额不正确

**解决方案：**
- 微信支付金额单位是"分"
- 确认金额转换：`amount * 100`
- 使用 `Math.round()` 确保整数

### 5. 商户号未绑定

**问题：** 返回"商户号未绑定"

**解决方案：**
- 登录微信支付商户平台
- 进入"产品中心" -> "AppID 账号管理"
- 绑定小程序 AppID

---

## 安全建议

1. **妥善保管密钥**
   - 不要将密钥提交到代码仓库
   - 使用环境变量配置
   - 定期更换密钥

2. **使用 HTTPS**
   - 所有支付接口必须使用 HTTPS
   - 回调地址必须是 HTTPS

3. **验证签名**
   - 所有回调必须验证签名
   - 防止恶意请求

4. **日志记录**
   - 记录所有支付请求和响应
   - 便于问题排查

5. **限制访问**
   - 回调接口添加访问控制
   - 防止恶意调用

---

## 参考资料

- [微信支付官方文档](https://pay.weixin.qq.com/wiki/doc/api/index.html)
- [微信支付 SDK](https://pay.weixin.qq.com/wiki/doc/api/download/PAY_SDK_V3.zip)
- [NestJS 配置管理](https://docs.nestjs.com/techniques/configuration)
- [微信支付开发者社区](https://developers.weixin.qq.com/community/pay)
