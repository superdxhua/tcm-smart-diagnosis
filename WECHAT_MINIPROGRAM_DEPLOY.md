# 微信小程序部署配置文件

## 1. 项目配置

将获取到的 AppID 填入 `project.config.json` 文件中：

```json
{
  "appid": "你的AppID",  // 替换为实际的 AppID
  ...
}
```

## 2. 后端服务器配置

微信小程序要求所有网络请求必须通过 HTTPS 域名，且需要在后台配置白名单。

### 需要配置的域名：
- `request` 域名：后端 API 地址
- `uploadFile` 域名：文件上传地址
- `downloadFile` 域名：文件下载地址

### 示例配置：
假设你的后端部署在：`https://your-api-server.com`

在微信小程序后台配置：
- 开发 → 开发管理 → 开发设置 → 服务器域名
- 添加以下域名：
  ```
  https://your-api-server.com
  ```

### 重要提示：
- 域名必须备案
- 必须使用 HTTPS
- 不能使用 IP 地址或 localhost

## 3. 构建小程序

### 本地构建：
```bash
npm run build:weapp
```

构建产物会生成在 `dist` 目录下。

## 4. 上传到微信

### 使用微信开发者工具：

1. 下载微信开发者工具
   - 下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

2. 打开项目
   - 选择"小程序"
   - 项目路径选择 `/workspace/projects/dist`
   - AppID 填入你的 AppID

3. 预览和调试
   - 点击"预览"按钮，生成二维码
   - 用微信扫描，在手机上预览

4. 上传代码
   - 点击"上传"按钮
   - 填写版本号和项目备注

## 5. 提交审核

### 在小程序后台操作：

1. 登录微信小程序后台
2. 版本管理 → 开发版本
3. 点击"提交审核"
4. 填写审核信息：
   - 功能页面描述
   - 选填服务类目（医疗相关需要资质）
5. 提交审核

### 审核时间：
- 个人主体：1-7 个工作日
- 企业主体：1-3 个工作日

## 6. 发布上线

审核通过后：
1. 登录小程序后台
2. 版本管理 → 审核版本
3. 点击"发布"

发布后，用户就可以在微信中搜索到你的小程序了。

## 7. 注意事项

### 个人主体限制：
- 不能涉及医疗诊断、处方开具等敏感功能
- 需要在免责声明中明确标注"仅供参考"
- 可能需要调整部分功能（如处方生成改为"参考建议"）

### 企业主体优势：
- 功能完整，可以上线医疗相关功能
- 支持支付功能（微信支付）
- 可以申请更多权限（如获取用户手机号）

### 推荐方案：
如果需要完整的医疗功能，建议：
1. 先用个人主体开发测试
2. 功能稳定后，注册企业账号
3. 迁移到企业账号发布

## 8. 快速开始（测试环境）

如果你想先在手机上测试，可以：

### 方法一：使用微信开发者工具预览
```bash
# 1. 构建小程序
npm run build:weapp

# 2. 打开微信开发者工具
# 3. 导入 dist 目录
# 4. AppID 使用测试号（touristappid）
# 5. 点击预览，扫码在手机上查看
```

### 方法二：使用小程序云开发（推荐快速测试）
1. 微信开发者工具 → 云开发
2. 开通云开发（免费额度）
3. 配置云函数作为后端
4. 避免配置服务器域名的麻烦

## 9. 部署后端到公网服务器

由于微信小程序需要 HTTPS 域名，你需要将后端部署到公网服务器：

### 推荐方案：

#### 方案 A：Render（免费）
1. 注册 Render 账号
2. 创建 Web Service
3. 连接 GitHub 仓库
4. 自动部署，获得 HTTPS 域名

#### 方案 B：Vercel（前端）+ Render（后端）
- 前端部署到 Vercel
- 后端部署到 Render
- 免费额度足够小规模使用

#### 方案 C：阿里云/腾讯云
- 购买云服务器
- 配置 SSL 证书
- 域名解析

## 10. 当前项目的特殊注意事项

### 需要调整的功能：

1. **处方生成功能**
   - 个人主体：改为"参考建议"，不能作为正式处方
   - 企业主体：需要医疗资质

2. **用户权限管理**
   - 个人主体：简化权限，只保留基础功能
   - 企业主体：完整权限系统

3. **支付充值**
   - 个人主体：不能使用微信支付
   - 企业主体：可以申请微信支付

## 11. 联系与支持

遇到问题可以查看：
- 微信小程序官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- Taro 官方文档：https://taro-docs.jd.com/
- 本项目技术支持：[客服中心](./src/pages/customer-service/index.tsx)

---

## 🎉 下一步操作

### 现在你可以：

1. **立即测试（无需注册）**
   ```bash
   npm run build:weapp
   # 然后用微信开发者工具打开 dist 目录，扫码预览
   ```

2. **注册正式账号（约30分钟）**
   - 访问 https://mp.weixin.qq.com/
   - 注册小程序账号
   - 获取 AppID

3. **配置后端服务器（1-2小时）**
   - 部署到 Render 或其他云服务
   - 配置 HTTPS 域名
   - 在小程序后台配置服务器域名

4. **提交审核发布（1-7天）**
   - 上传代码
   - 提交审核
   - 审核通过后发布

需要我帮你具体操作哪一步吗？
