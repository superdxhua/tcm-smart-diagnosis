# 微信支付测试文档

## 目录
1. [测试准备](#测试准备)
2. [配置状态检查](#配置状态检查)
3. [沙箱环境测试](#沙箱环境测试)
4. [正式环境测试](#正式环境测试)
5. [测试用例](#测试用例)
6. [常见问题排查](#常见问题排查)

---

## 测试准备

### 1. 确认服务器运行状态

```bash
# 检查服务器是否启动
curl -I http://localhost:3000

# 预期输出：HTTP/1.1 200 OK
```

### 2. 检查微信支付配置状态

```bash
curl -X GET http://localhost:3000/api/payment/config/wechat

# 预期输出：
{
  "code": 200,
  "msg": "success",
  "data": {
    "complete": false,  // 如果为 true，表示配置完整
    "isSandbox": false,
    "config": {
      "appId": "已设置/未设置",
      "mchId": "已设置/未设置",
      "apiKey": "已设置/未设置",
      "notifyUrl": "已设置/未设置",
      "certPath": "已设置/未设置",
      "certPassword": "已设置/未设置",
      "apiUrl": "https://api.mch.weixin.qq.com"
    }
  }
}
```

---

## 配置状态检查

### 测试目标

验证系统是否正确读取微信支付配置。

### 测试步骤

1. **未配置状态测试**
   ```bash
   curl -X GET http://localhost:3000/api/payment/config/wechat
   ```

   **预期结果**：
   - `complete: false`
   - 所有配置项显示"未设置"

2. **配置后状态测试**
   - 配置 `.env` 文件（参考 `.env.example`）
   - 重启服务器
   - 再次查询配置状态

   **预期结果**：
   - `complete: true`
   - 所有配置项显示"已设置"

---

## 沙箱环境测试

### 测试目标

在微信支付沙箱环境中测试支付流程。

### 测试准备

1. 配置沙箱环境变量：
   ```bash
   WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com/sandboxnew
   ```

2. 获取沙箱商户号和 API 密钥

### 测试用例 1：创建支付订单

```bash
curl -X POST http://localhost:3000/api/payment/create-package-order \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test_user_001",
    "packageId": "test_package_001",
    "paymentMethod": "wechat"
  }'
```

**预期输出**：
```json
{
  "code": 200,
  "msg": "订单创建成功",
  "data": {
    "orderNo": "PKG202501011234567890",
    "packageId": "test_package_001",
    "packageTitle": "月度会员",
    "amount": 29.9,
    "paymentMethod": "wechat",
    "status": "pending",
    "paymentUrl": "https://qr.alipay.com/...",
    "qrCode": "data:image/png;base64,..."
  }
}
```

### 测试用例 2：查询订单状态

```bash
curl -X GET "http://localhost:3000/api/payment/order/PKG202501011234567890"
```

**预期输出**：
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "orderNo": "PKG202501011234567890",
    "packageId": "test_package_001",
    "userId": "test_user_001",
    "amount": 29.9,
    "status": "pending",  // pending, paid, failed, refunded
    "paymentMethod": "wechat",
    "transactionId": null,
    "createdAt": "2025-01-01T12:34:56.789Z",
    "paidAt": null
  }
}
```

### 测试用例 3：申请退款

```bash
curl -X POST http://localhost:3000/api/payment/refund \
  -H "Content-Type: application/json" \
  -d '{
    "orderNo": "PKG202501011234567890",
    "refundAmount": 29.9,
    "refundReason": "用户申请退款"
  }'
```

**预期输出**：
```json
{
  "code": 200,
  "msg": "退款申请成功",
  "data": {
    "refundId": "REF202501011234567890",
    "orderNo": "PKG202501011234567890",
    "refundAmount": 29.9,
    "status": "SUCCESS",
    "refundReason": "用户申请退款"
  }
}
```

### 测试用例 4：查询退款

```bash
curl -X GET "http://localhost:3000/api/payment/refund/REF202501011234567890"
```

**预期输出**：
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "refundId": "REF202501011234567890",
    "status": "SUCCESS",
    "refundAmount": 29.9,
    "refundReceiveAccount": "用户零钱"
  }
}
```

---

## 正式环境测试

### 测试目标

在微信支付正式环境中测试支付流程。

### 测试准备

1. 配置正式环境变量：
   ```bash
   WECHAT_PAY_API_URL=https://api.mch.weixin.qq.com
   ```

2. 确认所有配置项已正确设置

3. **重要提示**：
   - 正式环境测试会产生真实的资金交易
   - 测试金额建议设置为 0.01 元
   - 测试完成后及时申请退款

### 测试用例

与沙箱环境测试用例相同，但需注意：
- 使用真实的订单金额
- 支付成功后会有实际的资金扣除
- 建议使用测试账号进行测试

---

## 测试用例

### 用例 1：创建充值订单

**描述**：创建一个充值订单，测试订单创建流程。

**步骤**：
```bash
curl -X POST http://localhost:3000/api/payment/create-order \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test_user_001",
    "amount": 100,
    "paymentMethod": "wechat"
  }'
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 返回订单号
- [ ] 返回支付二维码链接
- [ ] 订单状态为 `pending`

### 用例 2：获取用户余额

**描述**：查询用户余额，验证充值是否成功。

**步骤**：
```bash
curl -X GET "http://localhost:3000/api/payment/balance?userId=test_user_001"
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 返回正确的余额信息

### 用例 3：获取用户订单列表

**描述**：查询用户的所有订单。

**步骤**：
```bash
curl -X GET "http://localhost:3000/api/payment/orders?userId=test_user_001"
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 返回订单列表
- [ ] 订单信息完整

### 用例 4：按状态筛选订单

**描述**：查询特定状态的订单。

**步骤**：
```bash
curl -X GET "http://localhost:3000/api/payment/orders?userId=test_user_001&status=paid"
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 只返回指定状态的订单

### 用例 5：获取订单统计

**描述**：查询用户的订单统计数据。

**步骤**：
```bash
curl -X GET "http://localhost:3000/api/payment/orders/stats?userId=test_user_001"
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 返回订单总数、总金额等统计信息

### 用例 6：续费套餐

**描述**：测试套餐续费功能。

**步骤**：
```bash
curl -X POST http://localhost:3000/api/payment/renew-package \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "test_user_001",
    "packageId": "test_package_001",
    "paymentMethod": "wechat"
  }'
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 返回续费订单信息
- [ ] 续费订单类型正确

### 用例 7：支付回调

**描述**：模拟微信支付回调，验证回调处理逻辑。

**步骤**：
```bash
curl -X POST http://localhost:3000/api/payment/callback \
  -H "Content-Type: application/json" \
  -d '{
    "orderNo": "PKG202501011234567890",
    "transactionId": "4200001234567890123456789",
    "status": "paid"
  }'
```

**验证点**：
- [ ] 返回 HTTP 200
- [ ] 订单状态更新为 `paid`
- [ ] 用户权限/余额更新

---

## 常见问题排查

### 问题 1：配置状态查询失败

**症状**：
```bash
curl: (7) Failed to connect to localhost port 3000
```

**解决方案**：
1. 确认服务器是否启动
2. 检查端口 3000 是否被占用
3. 查看服务器日志

### 问题 2：配置显示"未设置"

**症状**：
```json
{
  "complete": false,
  "config": {
    "appId": "未设置",
    ...
  }
}
```

**解决方案**：
1. 确认 `.env` 文件存在
2. 检查环境变量是否正确配置
3. 重启服务器

### 问题 3：创建订单失败

**症状**：
```json
{
  "code": 400,
  "msg": "创建订单失败"
}
```

**解决方案**：
1. 检查请求参数是否完整
2. 检查用户 ID 是否存在
3. 检查套餐 ID 是否存在
4. 查看服务器日志

### 问题 4：支付回调未触发

**症状**：支付成功后订单状态未更新

**解决方案**：
1. 检查回调地址是否正确配置
2. 确认微信支付后台的回调地址设置
3. 检查服务器日志中的回调记录
4. 手动调用回调接口进行测试

### 问题 5：退款失败

**症状**：
```json
{
  "code": 400,
  "msg": "退款失败"
}
```

**解决方案**：
1. 检查订单是否已支付
2. 检查退款金额是否正确
3. 检查证书配置是否正确
4. 查看微信支付后台的退款记录

### 问题 6：签名验证失败

**症状**：
```json
{
  "msg": "签名验证失败"
}
```

**解决方案**：
1. 检查 API 密钥是否正确
2. 检查签名算法是否正确
3. 检查参数排序是否正确
4. 检查是否正确拼接 `&key=`

---

## 测试报告模板

### 测试环境
- 服务器地址：`http://localhost:3000`
- 测试环境：沙箱 / 正式
- 测试日期：`2025-01-01`

### 测试结果

| 用例编号 | 用例名称 | 测试结果 | 备注 |
|---------|---------|---------|------|
| TC001 | 创建支付订单 | ✅ 通过 | - |
| TC002 | 查询订单状态 | ✅ 通过 | - |
| TC003 | 申请退款 | ✅ 通过 | - |
| TC004 | 查询退款 | ✅ 通过 | - |
| TC005 | 获取用户余额 | ✅ 通过 | - |
| TC006 | 获取订单列表 | ✅ 通过 | - |
| TC007 | 续费套餐 | ✅ 通过 | - |

### 发现的问题
1. 无

### 建议
1. 建议在正式环境测试前完成沙箱环境测试
2. 建议使用自动化测试工具提高测试效率
3. 建议监控支付接口的响应时间和成功率

---

## 自动化测试脚本

### 创建测试订单脚本

创建 `test-payment.sh`:

```bash
#!/bin/bash

BASE_URL="http://localhost:3000/api/payment"
USER_ID="test_user_001"
PACKAGE_ID="test_package_001"

# 测试 1: 查询配置状态
echo "测试 1: 查询配置状态"
curl -s -X GET "$BASE_URL/config/wechat" | jq
echo

# 测试 2: 创建套餐订单
echo "测试 2: 创建套餐订单"
ORDER_RESPONSE=$(curl -s -X POST "$BASE_URL/create-package-order" \
  -H "Content-Type: application/json" \
  -d "{\"userId\":\"$USER_ID\",\"packageId\":\"$PACKAGE_ID\",\"paymentMethod\":\"wechat\"}")

echo "$ORDER_RESPONSE" | jq
ORDER_NO=$(echo "$ORDER_RESPONSE" | jq -r '.data.orderNo')
echo "订单号: $ORDER_NO"
echo

# 测试 3: 查询订单状态
echo "测试 3: 查询订单状态"
curl -s -X GET "$BASE_URL/order/$ORDER_NO" | jq
echo

# 测试 4: 模拟支付回调
echo "测试 4: 模拟支付回调"
curl -s -X POST "$BASE_URL/callback" \
  -H "Content-Type: application/json" \
  -d "{\"orderNo\":\"$ORDER_NO\",\"transactionId\":\"test_transaction_id\",\"status\":\"paid\"}" | jq
echo

# 测试 5: 再次查询订单状态
echo "测试 5: 再次查询订单状态"
curl -s -X GET "$BASE_URL/order/$ORDER_NO" | jq
echo

# 测试 6: 申请退款
echo "测试 6: 申请退款"
REFUND_RESPONSE=$(curl -s -X POST "$BASE_URL/refund" \
  -H "Content-Type: application/json" \
  -d "{\"orderNo\":\"$ORDER_NO\",\"refundAmount\":29.9,\"refundReason\":\"测试退款\"}")

echo "$REFUND_RESPONSE" | jq
REFUND_ID=$(echo "$REFUND_RESPONSE" | jq -r '.data.refundId')
echo "退款单号: $REFUND_ID"
echo

# 测试 7: 查询退款状态
echo "测试 7: 查询退款状态"
curl -s -X GET "$BASE_URL/refund/$REFUND_ID" | jq
echo

echo "所有测试完成！"
```

### 运行测试脚本

```bash
chmod +x test-payment.sh
./test-payment.sh
```

---

## 参考资料

- [微信支付官方文档](https://pay.weixin.qq.com/wiki/doc/api/index.html)
- [微信支付 API 文档](https://pay.weixin.qq.com/wiki/doc/api/index.html)
- [微信支付沙箱环境](https://pay.weixin.qq.com/wiki/doc/api/index.html)
