# 提示音文件说明

## 如何添加提示音

管理员审核页面的提示音功能需要使用音频文件。请按照以下步骤添加提示音：

### 方法 1：使用在线提示音（推荐）

1. 下载提示音文件
   - 可从以下网站免费下载：
     - [FreeSound](https://freesound.org/) - 搜索 "notification"
     - [SoundBible](https://soundbible.com/) - 搜索 "ding"
     - [Zapsplat](https://www.zapsplat.com/) - 免费音效库

2. 将文件重命名为 `notification.mp3`

3. 放置到项目目录
   ```bash
   # 放置到项目根目录的 public 文件夹中
   cp notification.mp3 /workspace/projects/public/assets/notification.mp3
   ```

### 方法 2：使用系统提示音（H5 环境）

在 H5 环境中，您也可以使用浏览器内置的提示音：

```typescript
// 修改 src/pages/admin-recharge/index.tsx
const playNotificationSound = () => {
  try {
    // 使用 Web Audio API 生成简单的提示音
    const audioContext = new (window.AudioContext || (window as any).webkitAudioContext)()
    const oscillator = audioContext.createOscillator()
    const gainNode = audioContext.createGain()

    oscillator.connect(gainNode)
    gainNode.connect(audioContext.destination)

    oscillator.frequency.value = 800 // 频率 800Hz
    oscillator.type = 'sine' // 正弦波

    gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)
    gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.5)

    oscillator.start(audioContext.currentTime)
    oscillator.stop(audioContext.currentTime + 0.5)
  } catch (error) {
    console.error('播放提示音异常:', error)
  }
}
```

### 方法 3：静音模式（默认）

如果暂时没有提示音文件，系统会自动使用静音模式，不会影响审核功能的使用。

提示音开关（右上角的 🔊 按钮）可以随时开启/关闭。

## 提示音文件要求

- 格式：MP3 或 WAV
- 时长：建议 0.5 - 2 秒
- 大小：建议小于 100KB
- 内容：清脆的提示音，如"叮"的一声

## 文件路径

前端代码中的引用路径：
```typescript
const audio = new Audio('/assets/notification.mp3')
```

实际文件应放置在：
```
/workspace/projects/public/assets/notification.mp3
```
