# PWA 添加到主屏幕 - 用户引导方案

## 重要说明：为什么不能自动添加？

### 浏览器安全限制

**所有现代浏览器都强制要求用户手动确认**才能将网页添加到主屏幕。这是不可逾越的安全限制，原因包括：

#### 1. 防止恶意滥用
如果网页可以自动添加到主屏幕：
- 恶意网站可以在用户不知情的情况下安装"应用"
- 滥用用户设备存储
- 欺骗用户以为是官方应用

#### 2. 保护用户隐私
浏览器强制要求用户同意，确保：
- ✅ 用户明确知道要添加什么
- ✅ 用户主动选择是否添加
- ✅ 防止滥用和欺诈

#### 3. 各平台限制

| 平台 | 浏览器 | 是否需要用户确认 |
|-----|--------|----------------|
| iOS | Safari | ✅ 必须手动点击"添加到主屏幕" |
| Android | Chrome | ✅ 必须手动点击"安装" |
| Android | Edge | ✅ 必须手动点击"安装" |
| PC | Chrome | ✅ 必须手动点击"安装应用" |
| 微信 | - | ✅ 必须在浏览器中手动添加 |

**结论：这是浏览器的硬性限制，没有任何方法可以绕过。**

---

## 我们的最佳实践方案

虽然不能自动添加，但我们通过多种方式引导用户，让添加体验更加友好：

### 方案 1：顶部导航栏引导按钮（已实现）

**位置**: 首页顶部导航栏，"每日签到"按钮旁边

**样式**: 渐变紫色按钮，醒目吸引眼球

**功能**:
- 点击后显示针对不同平台的安装指南
- iOS 显示 iOS 安装步骤
- Android 显示 Android 安装步骤
- PC 显示桌面安装步骤

**效果**:
```
┌────────────────────────────────────────────────────────┐
│ 中医智能好帮手  [📄 免责声明] [🎁 每日签到] [📱 添加到主屏幕] │
└────────────────────────────────────────────────────────┘
```

### 方案 2：自动提示（已优化）

**触发条件**:
1. ✅ 用户首次访问
2. ✅ 停留页面 10 秒以上（表明用户有兴趣）
3. ✅ 浏览器支持 PWA 安装
4. ✅ 7天内未拒绝过

**提示样式**: 精美的浮动对话框

```
┌─────────────────────────────────────┐
│ 📱 添加到主屏幕                    │
│                                     │
│ 将"中医智能好帮手"添加到主屏幕，    │
│ 体验更流畅                          │
│                                     │
│        [立即添加]  [暂不添加]       │
└─────────────────────────────────────┘
```

### 方案 3：智能去重（已实现）

**避免频繁打扰**:
- 用户点击"暂不添加"后，7天内不再提示
- 已安装的用户不再提示
- 每个浏览器独立记录

---

## 各平台安装指南

### iOS (iPhone/iPad)

**方法 1：自动提示**
1. 等待 10 秒后，会看到"添加到主屏幕"提示
2. 点击"立即添加"
3. 点击"添加"按钮

**方法 2：手动添加**
1. 访问 `https://zhongyihskhealth.com`
2. 点击底部的"分享"按钮（方框向上箭头）
3. 向下滚动，找到"添加到主屏幕"
4. 点击"添加"按钮

**方法 3：顶部按钮引导**
1. 点击首页顶部的"📱 添加到主屏幕"按钮
2. 按照弹窗中的步骤操作

### Android

**方法 1：自动提示**
1. 等待 10 秒后，会看到"添加到主屏幕"提示
2. 点击"立即添加"
3. 点击"添加"或"安装"按钮

**方法 2：手动添加**
1. 访问 `https://zhongyihskhealth.com`
2. 点击右上角的菜单按钮（三个点）
3. 选择"添加到主屏幕"或"安装应用"
4. 点击"添加"或"安装"按钮

**方法 3：顶部按钮引导**
1. 点击首页顶部的"📱 添加到主屏幕"按钮
2. 按照弹窗中的步骤操作

### PC (Windows/Mac)

**Chrome/Edge**:
1. 访问 `https://zhongyihskhealth.com`
2. 点击地址栏右侧的"安装应用"图标（"+"号或下载图标）
3. 点击"安装"按钮
4. 确认安装

---

## 为什么这样设计？

### 1. 尊重用户选择
- ✅ 不强制用户添加
- ✅ 提供明确的引导
- ✅ 允许用户拒绝

### 2. 避免打扰用户
- ✅ 延迟显示（10秒后）
- ✅ 7天内不再重复提示
- ✅ 已安装用户不再提示

### 3. 提供多种方式
- ✅ 自动提示
- ✅ 手动引导按钮
- ✅ 针对不同平台的详细指南

---

## 常见问题

### Q: 为什么不能自动添加？

A: 这是浏览器的硬性安全限制，防止恶意网站滥用。所有现代浏览器都强制要求用户手动确认。

### Q: 自动提示什么时候出现？

A:
1. 用户首次访问
2. 停留页面 10 秒以上
3. 浏览器支持 PWA 安装
4. 7天内未拒绝过

### Q: 如何关闭自动提示？

A: 点击"暂不添加"，7天内不会再显示。或者直接添加到主屏幕，安装后就不再提示了。

### Q: 添加到主屏幕后有什么好处？

A:
- ✅ 像小程序一样使用，全屏显示
- ✅ 启动更快，体验更流畅
- ✅ 部分功能可以离线使用
- ✅ 更好的用户体验

### Q: 如何卸载？

A: 和卸载普通应用一样，长按应用图标，然后删除即可。

---

## 技术实现

### 自动提示逻辑

```typescript
// 检查是否已安装
const isInstalled = window.matchMedia('(display-mode: standalone)').matches

// 检查用户是否拒绝过
const dismissedTime = localStorage.getItem('pwaInstallPromptDismissed')
const daysSinceDismissed = (Date.now() - parseInt(dismissedTime)) / (1000 * 60 * 60 * 24)

// 延迟 10 秒显示
setTimeout(() => {
  if (sessionTime >= 10 && !showPrompt) {
    setShowPrompt(true)
  }
}, 10000)
```

### 引导按钮逻辑

```typescript
// 检测平台
const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent)
const isAndroid = /Android/.test(navigator.userAgent)

// 显示对应平台的安装指南
Taro.showModal({
  title: isIOS ? 'iOS 用户安装指南' : 'Android 用户安装指南',
  content: isIOS ? 'iOS 步骤...' : 'Android 步骤...',
})
```

---

## 总结

### ✅ 我们能做到的

1. **友好的引导**
   - 顶部导航栏按钮
   - 自动提示对话框
   - 平台特定的安装指南

2. **智能的提示**
   - 延迟显示（10秒后）
   - 避免频繁打扰（7天去重）
   - 已安装用户不再提示

3. **多种方式**
   - 自动提示
   - 手动引导
   - 详细指南

### ❌ 我们做不到的

1. **自动添加到主屏幕**
   - 浏览器安全限制
   - 所有平台都强制要求用户确认

2. **绕过用户确认**
   - 这是硬性限制
   - 没有任何方法可以绕过

### 📊 最佳实践

我们的方案已经是最优的用户体验：
- ✅ 尊重用户选择
- ✅ 避免频繁打扰
- ✅ 提供多种引导方式
- ✅ 针对不同平台优化

---

**文档版本**: 1.0.0
**更新时间**: 2025-01-09
**适用版本**: v1.0.0+
