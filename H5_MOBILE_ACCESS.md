# 📱 手机访问 H5 版本 - 详细配置指南

## 🎯 目标

让手机浏览器能够访问电脑上的 H5 版本

---

## 📋 前提条件

1. ✅ 电脑和手机连接**同一个 WiFi**
2. ✅ 已启动开发服务器（`coze dev`）
3. ✅ 电脑防火墙允许访问

---

## 🔍 第一步：查找电脑 IP 地址

### Windows

#### 方法 1：使用命令提示符

1. 按 `Win + R`，输入 `cmd`，回车
2. 输入：
   ```bash
   ipconfig
   ```
3. 找到"IPv4 地址"：
   ```
   IPv4 地址 . . . . . . . . . . . : 192.168.1.100
   ```
4. 记下这个地址：`192.168.1.100`

#### 方法 2：使用设置

1. 打开"设置"
2. 点击"网络和 Internet"
3. 点击"属性"
4. 找到"IPv4 地址"

---

### Mac

#### 方法 1：使用终端

1. 打开"终端"（Command + 空格，输入"终端"）
2. 输入：
   ```bash
   ifconfig
   ```
3. 找到"inet"开头的地址：
   ```
   inet 192.168.1.100 netmask 0xffffff00 broadcast 192.168.1.255
   ```
4. 记下这个地址：`192.168.1.100`

#### 方法 2：使用系统偏好设置

1. 打开"系统偏好设置"
2. 点击"网络"
3. 找到 IP 地址

---

### Linux

#### 方法 1：使用终端

1. 打开终端
2. 输入：
   ```bash
   ifconfig
   ```
3. 找到"inet"地址

#### 方法 2：使用 ip 命令

```bash
ip addr show
```

---

## 📱 第二步：手机访问

### iPhone（iOS）

#### 使用 Safari 浏览器

1. 打开 Safari 浏览器
2. 在地址栏输入：
   ```
   http://192.168.1.100:5000
   ```
3. 点击"前往"（Go）
4. 等待页面加载

#### 添加到主屏幕（获得 APP 体验）

1. 点击底部的"分享"按钮（方框向上箭头）
2. 向下滚动，点击"添加到主屏幕"
3. 点击"添加"
4. 在主屏幕找到图标，点击打开

**效果**：
- ✅ 全屏运行，无浏览器地址栏
- ✅ 像原生 APP 一样的体验
- ✅ 支持离线访问（如果配置了 PWA）

---

### Android

#### 使用 Chrome 浏览器

1. 打开 Chrome 浏览器
2. 在地址栏输入：
   ```
   http://192.168.1.100:5000
   ```
3. 点击"前往"
4. 等待页面加载

#### 添加到主屏幕（获得 APP 体验）

1. 点击浏览器菜单（右上角三个点）
2. 点击"添加到主屏幕"
3. 点击"添加"
4. 在主屏幕找到图标，点击打开

---

## 🔥 常见问题解决

### 问题 1：手机无法连接，显示"无法访问此网站"

#### 可能原因：防火墙阻止

**Windows 解决方案**：

**方法 1：临时关闭防火墙（测试用）**
1. 打开"控制面板"
2. 点击"Windows Defender 防火墙"
3. 点击"启用或关闭 Windows Defender 防火墙"
4. 关闭专用网络和公用网络的防火墙
5. 再次尝试手机访问
6. 成功后记得重新打开防火墙

**方法 2：添加防火墙规则（推荐）**
```bash
# 管理员身份打开命令提示符
# 运行以下命令：
netsh advfirewall firewall add rule name="Taro H5" dir=in action=allow protocol=TCP localport=5000
```

**方法 3：通过图形界面添加**
1. 打开"Windows Defender 防火墙"
2. 点击"高级设置"
3. 点击"入站规则"
4. 点击"新建规则"
5. 选择"端口" → "下一步"
6. 选择"TCP"，输入特定本地端口：`5000`
7. 选择"允许连接"
8. 全选（域、专用、公用）
9. 命名为"Taro H5"
10. 完成

---

**Mac 解决方案**：

1. 打开"系统偏好设置"
2. 点击"安全性与隐私"
3. 点击"防火墙"选项卡
4. 点击"防火墙选项"
5. 确保允许 Node.js 的入站连接
6. 点击"确定"

---

**Linux 解决方案**：

```bash
# Ubuntu/Debian
sudo ufw allow 5000

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
```

---

### 问题 2：连接超时

#### 可能原因：网络问题

**解决方案**：

1. **检查 WiFi 连接**
   - 确保电脑和手机在同一 WiFi
   - 尝试断开重连 WiFi

2. **检查服务器状态**
   - 在电脑浏览器访问：http://localhost:5000
   - 如果电脑也无法访问，说明服务器未启动
   - 重新运行：`coze dev`

3. **检查 IP 地址**
   - 重新查找电脑 IP 地址
   - 确保输入的 IP 地址正确

4. **重启服务器**
   - 按 `Ctrl+C` 停止服务器
   - 重新运行：`coze dev`

---

### 问题 3：页面加载很慢

#### 可能原因：网络速度或电脑性能

**解决方案**：

1. **优化 WiFi**
   - 确保信号强（靠近路由器）
   - 避免网络拥堵

2. **关闭其他程序**
   - 关闭占用网络的程序（下载、视频等）
   - 关闭占用 CPU 的程序

3. **刷新页面**
   - 下拉刷新页面
   - 或清除浏览器缓存

4. **等待首次加载**
   - 首次加载较慢是正常的
   - 后续加载会更快（有缓存）

---

### 问题 4：页面显示不正常

#### 可能原因：浏览器兼容性

**解决方案**：

1. **清除浏览器缓存**
   - iPhone Safari：设置 → Safari → 清除历史记录与网站数据
   - Android Chrome：菜单 → 历史记录 → 清除浏览数据

2. **更新浏览器**
   - iPhone：更新 iOS 系统
   - Android：更新 Chrome 到最新版

3. **尝试其他浏览器**
   - iPhone：尝试 Chrome、Firefox
   - Android：尝试 Firefox、Edge

4. **检查电脑版本**
   - 在电脑浏览器访问：http://localhost:5000
   - 如果电脑也不正常，说明是代码问题

---

### 问题 5：后端接口失败

#### 可能原因：手机无法访问 localhost

**解决方案**：

**临时方案（测试用）**：

在 `.env.local` 文件中设置后端地址为电脑 IP：

```bash
PROJECT_DOMAIN=http://192.168.1.100:3000
```

重启服务器：
```bash
coze dev
```

**永久方案（推荐）**：

部署到云端（见 H5_QUICK_START.md 中的云端部署方案）

---

## 🌟 最佳实践

### 1. 使用稳定的 WiFi

- 确保信号强
- 避免网络拥堵
- 使用 5G WiFi（如果支持）

### 2. 优化性能

- 关闭不必要的后台程序
- 使用性能好的电脑
- 定期清理缓存

### 3. 提供更好的体验

- 添加到主屏幕
- 使用全屏模式
- 离线支持（PWA）

### 4. 安全建议

- 不要在公共 WiFi 使用（涉及隐私数据）
- 定期更新系统
- 使用 HTTPS（云端部署）

---

## 📊 不同场景的配置

| 场景 | 使用方法 | 适合人群 |
|------|---------|---------|
| 开发测试 | 本地 WiFi | 开发者、测试人员 |
| 团队测试 | 公司内网 | 团队成员 |
| 公开访问 | 云端部署 | 所有用户 |
| 正式上线 | 云端部署 + HTTPS | 正式用户 |

---

## 🎉 成功标志

当你在手机浏览器中看到以下内容，说明配置成功：

1. ✅ 页面正常显示
2. ✅ 可以正常操作
3. ✅ 数据可以加载
4. ✅ 功能正常工作

---

## 📞 需要帮助？

如果以上方法都不行：

1. **检查日志**
   - 查看服务器控制台是否有错误
   - 查看手机浏览器控制台（开发者工具）

2. **联系技术支持**
   - 邮箱：support@example.com
   - 电话：400-xxx-xxxx

---

**最后更新**：2026年2月
**版本**：1.0.0
