# 智能中医辅助诊疗 - 快速开始指南

## 🚀 5 分钟快速上手

### 方法一：使用网页版（最简单）

1. **打开浏览器**
   - 访问：http://localhost:5000（本地）
   - 或访问：https://your-tcm-app.netlify.app（在线）

2. **开始使用**
   - 注册账号
   - 开始使用智能诊疗功能

**优点**：无需下载，即开即用
**缺点**：需要网络连接

---

### 方法二：使用构建脚本（推荐）

#### 1. 构建 H5 版本

```bash
# 检查脚本是否可执行
chmod +x build.sh

# 构建 H5 版本
./build.sh web
```

构建完成后，H5 版本位于 `dist-web/` 目录。

#### 2. 部署 H5 到 Netlify

```bash
# 方法一：拖拽上传（最简单）
# 1. 访问 https://app.netlify.com/drop
# 2. 将 dist-web 文件夹拖拽上传
# 3. 等待部署完成，获得网站地址

# 方法二：使用 Git
# 1. 将代码推送到 GitHub
# 2. 在 Netlify 中连接仓库
# 3. 设置发布目录为 dist-web
# 4. 点击部署
```

#### 3. 生成手机 App

**使用 GoNative（5 分钟搞定）**：

1. 访问：https://gonative.io
2. 注册免费账号
3. 输入 H5 网站地址
4. 上传图标（1024x1024 PNG）
5. 点击"Generate"
6. 等待 5-10 分钟
7. 下载 APK 或 IPA

详细步骤请参考：[手机版App生成指南.md](./手机版App生成指南.md)

#### 4. 生成电脑 App

**Windows/macOS/Linux**：

```bash
# 进入 electron 目录
cd electron

# 安装依赖
npm install

# 构建 Windows 版本（在 Windows 上）
npm run build:win

# 构建 macOS 版本（在 Mac 上）
npm run build:mac

# 构建 Linux 版本（在 Linux 上）
npm run build:linux
```

详细步骤请参考：[electron/README.md](./electron/README.md)

---

## 📋 完整构建流程

### 步骤 1：准备环境

```bash
# 检查 Node.js 版本
node --version  # 应该 >= 16.0.0

# 检查包管理器
npm --version
# 或
pnpm --version
```

### 步骤 2：构建所有版本

```bash
# 使用构建脚本
./build.sh all
```

这会依次构建：
- H5 网页版
- 微信小程序版本
- 电脑版（Electron，根据操作系统）

### 步骤 3：部署和打包

#### 部署 H5 到 Netlify

```bash
# 拖拽上传 dist-web 文件夹到
# https://app.netlify.com/drop
```

#### 打包手机 App

参考：[手机版App生成指南.md](./手机版App生成指南.md)

#### 打包电脑 App

```bash
cd electron
npm run build:win  # Windows
npm run build:mac  # macOS
npm run build:linux  # Linux
```

---

## 🎯 常见任务

### 只构建 H5 版本

```bash
./build.sh web
# 或
pnpm build:web
```

### 只构建小程序版本

```bash
./build.sh weapp
# 或
pnpm build:weapp
```

### 只构建电脑版

```bash
./build.sh electron
# 或
cd electron && npm run build:win
```

### 本地测试 H5 版本

```bash
# 启动开发服务器
pnpm dev

# 访问 http://localhost:5000
```

---

## 📱 手机 App 生成

### Android APK

**最快方法（5 分钟）**：

1. 访问：https://gonative.io
2. 注册账号
3. 输入 H5 网站地址
4. 上传图标
5. 点击"Generate"
6. 下载 APK

**详细步骤**：[手机版App生成指南.md](./手机版App生成指南.md)

### iOS IPA

**方法一：GoNative（推荐）**

1. 访问：https://gonative.io
2. 注册账号
3. 输入 H5 网站地址
4. 上传图标
5. 点击"Generate"
6. 下载 IPA
7. 使用 TestFlight 测试

**方法二：Xcode（开发者账号）**

1. 需要 Apple Developer 账号（$99/年）
2. 使用 Xcode 打包
3. 提交到 App Store

---

## 💻 电脑 App 生成

### Windows

```bash
cd electron
npm install
npm run build:win
```

安装包位于 `electron/dist/` 目录。

### macOS

```bash
cd electron
npm install
npm run build:mac
```

安装包位于 `electron/dist/` 目录。

**注意**：需要在 Mac 电脑上构建。

### Linux

```bash
cd electron
npm install
npm run build:linux
```

安装包位于 `electron/dist/` 目录。

---

## 🎨 准备图标和启动页

### 电脑版图标

按照 `electron/图标说明.md` 准备图标：
- Windows: `icon.ico`（256x256 像素）
- macOS: `icon.icns`（1024x1024 像素）
- Linux: `icon.png`（512x512 像素）

### 手机版图标

- Android: `icon.png`（1024x1024 像素）
- iOS: `icon.png`（1024x1024 像素）

### 启动页

- Android: `splash.png`（1080x1920 像素）
- iOS: `splash.png`（1242x2208 像素）

---

## 🚨 常见问题

### 1. 构建失败

**问题**：`build.sh` 无法执行

**解决**：
```bash
chmod +x build.sh
```

### 2. Node.js 版本不对

**问题**：Node.js 版本太旧

**解决**：
```bash
# 使用 nvm 安装最新版
nvm install 18
nvm use 18
```

### 3. 依赖安装失败

**问题**：`npm install` 或 `pnpm install` 失败

**解决**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules
rm -rf node_modules

# 重新安装
npm install
```

### 4. 图标文件缺失

**问题**：构建时提示找不到图标文件

**解决**：
- 按照 `electron/图标说明.md` 准备图标
- 或暂时注释掉图标配置

### 5. H5 版本构建被中断

**问题**：`pnpm build:web` 被 Killed

**解决**：
- 增加系统内存
- 或使用更轻量的构建方式
- 或直接使用开发服务器 `pnpm dev`

### 6. 手机 App 无法安装

**问题**：Android APK 无法安装

**解决**：
- 开启"安装未知来源应用"
- 或重新生成 APK

---

## 📚 文档导航

| 文档 | 说明 |
|------|------|
| [多平台版本总览.md](./多平台版本总览.md) | 多平台版本总览 |
| [electron/README.md](./electron/README.md) | 电脑版详细文档 |
| [electron/图标说明.md](./electron/图标说明.md) | 电脑版图标制作 |
| [手机版App生成指南.md](./手机版App生成指南.md) | 手机版生成指南 |
| [downloads/index.html](./downloads/index.html) | 多平台下载页面 |

---

## 🎯 推荐流程

### 最简单流程（5 分钟）

1. **使用网页版**
   - 访问：http://localhost:5000
   - 开始使用

### 标准流程（30 分钟）

1. **构建 H5 版本**（5 分钟）
   ```bash
   ./build.sh web
   ```

2. **部署到 Netlify**（5 分钟）
   - 拖拽上传 dist-web 文件夹

3. **生成 Android APK**（15 分钟）
   - 使用 GoNative
   - 上传 H5 网站地址
   - 下载 APK

4. **测试和发布**（5 分钟）
   - 安装测试
   - 发布到应用商店

### 完整流程（2 小时）

1. **构建所有版本**（30 分钟）
   ```bash
   ./build.sh all
   ```

2. **准备图标和启动页**（30 分钟）
   - 设计图标
   - 准备启动页

3. **生成手机 App**（30 分钟）
   - 使用 GoNative 生成 Android APK
   - 使用 GoNative 生成 iOS IPA

4. **生成电脑 App**（30 分钟）
   ```bash
   cd electron
   npm run build:win
   npm run build:mac
   npm run build:linux
   ```

---

## 🆘 获取帮助

- **文档**：[多平台版本总览.md](./多平台版本总览.md)
- **邮箱**：support@tcm-assistant.com
- **GitHub Issues**: https://github.com/your-repo/issues

---

## 🎉 完成！

恭喜您已经完成了智能中医辅助诊疗系统的多平台构建！

现在您可以：
- ✅ 在网页版中使用
- ✅ 在手机 App 中使用
- ✅ 在电脑 App 中使用
- ✅ 部署到应用商店

祝您使用愉快！🎊
